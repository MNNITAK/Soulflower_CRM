# Azure SQL connection (SQL Login)
DB_SERVER=soulflower.database.windows.net
DB_NAME=your_database_name
DB_USER=your_sql_username
DB_PASSWORD=your_sql_password
DB_PORT=1433

# HTTP server
PORT=4000

# Allowed origin for the Next.js frontend
CORS_ORIGIN=http://localhost:3000
